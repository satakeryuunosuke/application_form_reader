# 受講確認票 処理システム — 実装完了サマリー

## 1. 成果物一覧

完全クライアントサイド（外部通信ゼロ・無料・GitHub Pages対応）の受講確認票処理システムです。

### 主要ファイル構成

| ファイル / ディレクトリ | 内容・役割 |
| :--- | :--- |
| [`index.html`](./index.html) | アプリケーションのエントリーポイント（SPA） |
| [`CONCEPT_DESIGN.md`](./CONCEPT_DESIGN.md) | 概念設計書（確定版） |
| [`README.md`](./README.md) | プロジェクト概要・操作マニュアル |
| `css/` | デザイントークン、ベース、共通コンポーネント、各画面スタイル |
| `js/` | ルーティング、IndexedDB層、スキャンエンジン、チェックボックス判定、各画面コントローラー、バリデータ、CSV/Excel入出力 |
| `lib/` | 完全オフライン動作のためのローカルライブラリ群（Dexie.js, SheetJS, pdf.js, ZXing） |
| `templates/` | 配布用生徒データ取込用CSVテンプレート |

---

## 2. 実装した機能の詳細

### ① プロジェクト管理 & 進行中優先表示 & 完了時編集ロック（★追加・改修）
- **進行中・完了ステータス管理**:
  - 各プロジェクトに「進行中（`status: 進行中`）」と「完了（`status: 完了`）」の概念を導入。
  - 新規プロジェクト作成時は自動で「進行中」に設定。
- **ホーム画面での進行中優先表示**:
  - **上部**: 🚀 **進行中のプロジェクト (N件)** を大きくまとめて優先表示。
  - **下部**: ✅ **完了・終了したプロジェクト (M件)** を整理して表示（完了カードは落ち着いたトーンで視覚的に識別可能）。
  - **フィルタータブ**: 「すべて」「🚀 進行中」「✅ 完了・終了」のワンクリック切り替えが可能。
- **完了プロジェクトの編集ロック保護（データ保護）**:
  - **閲覧・出力の維持**: 完了したプロジェクトでも「提出状況一覧」の閲覧や CSV/Excel 出力は自由に行えます。
  - **スキャン読取のロック**: スキャンアップロードおよび承認登録画面がロックされ、誤った上書きを防止します。
  - **手動登録・変更のロック**: フォームの各項目および保存ボタンが無効化（disabled）され、閲覧専用となります。
  - **書式調整のロック**: 書式・読取位置調整ボタンが無効化されます。
  - **DB層の二重保護**: `DB.saveSubmission` においても対象プロジェクトが完了の場合は例外を投げてデータ変更を二重に阻止します。
- **ワンクリック ステータス切り替え**:
  - **ホーム画面の各カード**: 「🏁 完了にする」／「🔄 進行中に戻す」ボタンから即座に切り替え可能。
  - **プロジェクト詳細画面のヘッダー**: ステータスバッジ（`🟢 進行中` / `🏁 完了`）と「🏁 完了にする」／「🔄 進行中に戻す」ボタンを配置し、詳細画面内からも自在に切り替え可能。

### ② プロジェクト新規作成ウィザード（Step 1〜3）
- **Step 1: 基本情報**:
  - 実施年度: プルダウン選択（今年度を自動初期選択、過去・未来年度への自由な変更に対応）
  - 学年: 1〜6年のプルダウン
  - 講習名: 春期・夏期・冬期のプルダウン
- **Step 2: 生徒CSV読込**:
  - 「テンプレートCSV取得」ボタンで空のテンプレート（科目列含む）をダウンロード可能
  - 先頭5列（日能研番号, 氏名, 氏名カナ, クラス, 科目）を読み込み、未指定時は「4科」に自動フォールバック
  - ドラッグ＆ドロップによる読込とプレビュー表示（クラス・科目バッジ）
- **Step 3: 読取設定**:
  - バーコードアンカー方式の初期テンプレート設定と確認

### ③ 読み取り・承認（スキャンタブ）
- **複数ページ連続スキャンPDFの一括読取**:
  - pdf.js で各ページを描画し、ZXing で CODE 39 バーコードを自動検出
  - バーコード中心点をアンカーとしてチェックボックス領域を特定し、黒画素率判定
- **2ペイン連続承認UI**:
  - **左ペイン**: スキャン実寸画像ビューア（拡大・縮小・パン操作対応）
  - **右ペイン**: 
    - 作業者（職員名）プルダウン選択
    - 読取日能研番号（手動修正＋13の倍数チェックデジット検証）
    - 生徒情報の自動照合表示（氏名・カナ・所属クラス・所属科目）
    - 受講内容ラジオボタン（「変更なし」/「変更あり」）
    - **「変更あり」選択時**: クラス選択（クラス変更なし / 変更先クラス / 🚫 非受講）＋ **変更先科目（4科 / 2科）**
    - 特記事項メモ入力
    - **ショートカットキー操作**: Enterキーで承認・次へ、Spaceキーでスキップ

### ④ 提出状況一覧タブ
- **サマリーカード**: 全登録生徒数、変更なし、変更あり、非受講、未提出のリアルタイム集計（クリックで即座にフィルタ連動）
- **一覧テーブル**:
  - カラム: 日能研番号, 氏名, 氏名カナ, 所属クラス, **所属科目 (4科/2科)**, 提出ステータス, **受講クラス (確定)**, **受講科目 (確定: 4科/2科/非受講)**, 受付方法, 承認者, 日時, 特記事項, 変更履歴
- **フィルタ & 検索**: ステータス別絞り込み、クラス別（前・後）絞り込み、**科目別（前・後）絞り込み**、インクリメンタル検索
- **エクスポート**: 表示中の絞り込みデータをそのまま **CSV** / **Excel (.xlsx)**（所属科目・受講科目含む）としてワンクリック出力

### ⑤ 手動登録・変更タブ
- 紙の紛失・口頭・電話連絡・再変更を記録
- 生徒のインクリメンタル検索（日能研番号または氏名・カナ、所属クラス・科目バッジ表示）
- 連絡方法（口頭 / 電話 / その他）、受付日時の記録
- 変更内容（変更なし / 変更先クラス・変更先科目 / 非受講）および特記事項の更新

### ⑥ システム設定画面
- **職員マスタ管理**: 承認作業者の追加・削除
- **交換票（受講確認票）の共通既定書式設定（★新設）**:
  - 全プロジェクト共通の初期書式（バーコードアンカーからの「変更なし」「変更あり」枠の左右/上下オフセット、正方形サイズ、判定閾値）を設定画面から直接キャリブレーションして保存。
  - サンプル帳票やお持ちのPDF/画像を読み込んでリアルタイムにプレビュー・テスト調整が可能。
  - 「💾 共通既定書式を保存」で設定を永続化し、新規プロジェクト作成ウィザード（Step 3）に自動で初期値として反映。
  - 「🔄 システム標準（初期値）に戻す」ボタンでいつでも初期状態に復元可能。
- **3年経過アーカイブ管理**: 3年以上経過した古いプロジェクトの検出と整理・削除
- **完全ローカル バックアップ & 復元**: 全データを1つのJSONファイルとしてエクスポート / インポート

