# エージェント運用ルール (Agent Guidelines)

## 📌 バージョン管理規約 (Versioning Rules)

本リポジトリで機能追加、UI改善、不具合修正、ロジック変更などの改修を実施した際は、**必ず適切にバージョンを引き上げてください**。

### 1. バージョン採番基準（Semantic Versioning）
- **PATCH (例: v1.3.0 → v1.3.1)**:
  - 不具合（バグ）の修正
  - 軽微なUI調整、表示文言の修正、スタイル微調整
  - 既存動作に影響を与えない内部リファクタリング
- **MINOR (例: v1.3.0 → v1.4.0)**:
  - 新機能の追加（新規モーダル、新規連携機能、新規出力フォーマットなど）
  - 既存画面や同期・集計機能の大幅な機能拡張・改善
  - 後方互換性を維持した機能改修
- **MAJOR (例: v1.3.0 → v2.0.0)**:
  - 後方互換性のない破壊的変更（DBスキーマの根本的刷新、データ移行を要する設計変更など）

### 2. 改修完了時に必ず更新する対象ファイル
バージョンを引き上げる際は、以下のファイルを一括で更新してください。

1. **`js/version.js`**:
   - `APP_VERSION`: 新しいバージョン文字列（例: `'v1.3.1'`）
   - `APP_BUILD_DATE`: 改修実施日（`YYYY-MM-DD` 形式）
2. **`index.html`**:
   - `<span class="brand-version" id="header-app-version">vX.Y.Z</span>` のバージョン文字列
3. **`README.md`**:
   - 1行目のタイトル末尾 `vX.Y.Z` のバージョン文字列
4. **`CONCEPT_DESIGN.md`**（仕様書関連の改修がある場合）:
   - タイトル末尾のバージョン表記
